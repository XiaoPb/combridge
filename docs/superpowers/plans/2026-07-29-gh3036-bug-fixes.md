# GH3036 模块 Bug 修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 GH3036 模块的四个关键问题：BLE UUID 自动复位、串口波特率手动输入、BLE 写入方式错误、波形显示不连续。

**Architecture:**
- 问题1（UUID复位）：前端 Gh3036ChannelConfig 组件的 useEffect 过度触发，需要添加防抖或手动编辑标记
- 问题2（波特率输入）：前端 Select 改为 AutoComplete，后端添加验证
- 问题3（BLE写入）：后端 gatt_client 需根据特征属性选择 write_with_response 或 write_without_response
- 问题4（波形不连续）：检查前端数据流和图表渲染逻辑，确保实时更新

**Tech Stack:** React 19, TypeScript, Zustand, Ant Design 6.3.5, Rust (Tauri 2.0), bluest, ECharts

---

## 范围检查

本计划涉及4个相对独立的子系统：
1. **BLE 通道配置**（问题1和3）- 前端组件 + 后端BLE写入
2. **串口配置**（问题2）- 前端输入组件 + 后端验证
3. **波形显示**（问题4）- 前端数据流 + 图表渲染

虽然涉及不同模块，但它们都是 GH3036 模块的 Bug 修复，耦合度较低，可以在一个计划中完成。

---

## 文件结构

**前端文件：**
- `src/pages/Protocol/Gh3036ChannelConfig.tsx` - BLE 通道配置（问题1）
- `src/pages/Serial/SerialSettings.tsx` - 串口设置（问题2）
- `src/pages/Gh3036/MonitorTab.tsx` - 波形显示容器（问题4）
- `src/pages/Waveform/MultiLineChart.tsx` - 波形图表组件（问题4）
- `src/pages/Gh3036/monitorChartData.ts` - 波形数据构建（问题4）
- `src/stores/gh3036Store.ts` - GH3036 状态管理（问题1）
- `src/types/serial.ts` - 串口类型定义（问题2）

**后端文件：**
- `src-tauri/src/device/ble/native/gatt_client.rs` - BLE GATT 客户端（问题3）
- `src-tauri/src/device/ble/ble_traits.rs` - BLE trait 定义（问题3）
- `src-tauri/src/device/serial/serial_config.rs` - 串口配置验证（问题2）

**测试文件：**
- `src/stores/gh3036FrameBuffer.test.ts` - 现有测试文件
- `src-tauri/src/device/ble/native/gatt_client_test.rs` - 需要创建（问题3）

---

## Task 1: 修复 BLE UUID 自动复位问题

**问题分析：**
在 `Gh3036ChannelConfig.tsx` 的第61-98行，有一个 useEffect 会在 BLE 设备或特征变化时自动更新配置。问题在于 line 79-80：
```typescript
txChar: txChar?.uuid ?? channelConfig.txChar,
rxChar: rxChar?.uuid ?? channelConfig.rxChar,
```
这会导致用户手动输入的 UUID 被自动发现的 UUID 覆盖。

**解决方案：**
添加手动编辑标记，当用户手动修改 UUID 后，不再自动更新。

### Task 1.1: 添加手动编辑状态标记

**Files:**
- Modify: `src/stores/gh3036Store.ts`

- [ ] **Step 1: 在 Gh3036ChannelConfigState 添加手动编辑标记**

```typescript
export type Gh3036ChannelConfigState = {
  connectionType: string;
  serialPort: string;
  bleDevice: string;
  txChar: string;
  rxChar: string;
  txCharManuallyEdited: boolean;  // 新增：TX UUID 手动编辑标记
  rxCharManuallyEdited: boolean;  // 新增：RX UUID 手动编辑标记
};
```

在 `src/stores/gh3036Store.ts` 第43-49行，修改类型定义。

- [ ] **Step 2: 更新初始状态**

在 `src/stores/gh3036Store.ts` 第258-264行，修改初始状态：

```typescript
channelConfig: {
  connectionType: 'serial',
  serialPort: '',
  bleDevice: '',
  txChar: '00000004-0000-1000-8000-00805f9b34fb',
  rxChar: '00000003-0000-1000-8000-00805f9b34fb',
  txCharManuallyEdited: false,
  rxCharManuallyEdited: false,
},
```

- [ ] **Step 3: 添加状态更新方法**

在 `src/stores/gh3036Store.ts` 的 interface Gh3036State 部分（约第207行），添加方法定义：

```typescript
setTxCharManuallyEdited: (edited: boolean) => void;
setRxCharManuallyEdited: (edited: boolean) => void;
```

在 store 实现部分（约第580行后），添加实现：

```typescript
setTxCharManuallyEdited: (edited) => set((state) => ({
  channelConfig: { ...state.channelConfig, txCharManuallyEdited: edited }
})),

setRxCharManuallyEdited: (edited) => set((state) => ({
  channelConfig: { ...state.channelConfig, rxCharManuallyEdited: edited }
})),
```

- [ ] **Step 4: 提交 Task 1.1 的修改**

```bash
git add src/stores/gh3036Store.ts
git commit -m "feat(gh3036): 添加BLE UUID手动编辑状态标记"
```

### Task 1.2: 修改 Gh3036ChannelConfig 组件逻辑

**Files:**
- Modify: `src/pages/Protocol/Gh3036ChannelConfig.tsx`

- [ ] **Step 1: 添加手动编辑标记的引用**

在 `Gh3036ChannelConfig.tsx` 第16-24行，修改 store 引用：

```typescript
const {
  channelConfig,
  csvConfig,
  loadChannelConfig,
  updateChannelConfig,
  configureTxChannel,
  configureRxChannel,
  updateCsvConfig,
  setTxCharManuallyEdited,  // 新增
  setRxCharManuallyEdited,  // 新增
} = useGh3036Store();
```

- [ ] **Step 2: 修改 useEffect 自动更新逻辑**

在 `Gh3036ChannelConfig.tsx` 第61-98行，修改 useEffect：

```typescript
useEffect(() => {
  if (!currentBleDevice || channelConfig.connectionType !== 'ble') return;

  const tab = bleDeviceTabs[currentBleDevice];
  const characteristics = tab?.characteristics ?? [];

  // 只在未手动编辑时自动选择特征
  let txChar = channelConfig.txChar;
  let rxChar = channelConfig.rxChar;

  if (!channelConfig.txCharManuallyEdited) {
    const txCharCandidate = preferCharacteristic(
      characteristics,
      '00000004',
      (char) => char.properties.write || char.properties.writeWithoutResponse
    );
    txChar = txCharCandidate?.uuid ?? channelConfig.txChar;
  }

  if (!channelConfig.rxCharManuallyEdited) {
    const rxCharCandidate = preferCharacteristic(
      characteristics,
      '00000003',
      (char) => char.properties.notify || char.properties.indicate
    );
    rxChar = rxCharCandidate?.uuid ?? channelConfig.rxChar;
  }

  const nextConfig = {
    bleDevice: currentBleDevice,
    txChar,
    rxChar,
  };

  if (
    nextConfig.bleDevice !== channelConfig.bleDevice ||
    nextConfig.txChar !== channelConfig.txChar ||
    nextConfig.rxChar !== channelConfig.rxChar
  ) {
    void updateChannelConfig(nextConfig);
  }
}, [
  currentBleDevice,
  bleDeviceTabs,
  channelConfig.connectionType,
  channelConfig.bleDevice,
  channelConfig.txChar,
  channelConfig.rxChar,
  channelConfig.txCharManuallyEdited,
  channelConfig.rxCharManuallyEdited,
  updateChannelConfig,
]);
```

- [ ] **Step 3: 在 Input onChange 中设置手动编辑标记**

在 `Gh3036ChannelConfig.tsx` 第218-227行，修改 TX UUID Input：

```typescript
<Input
  size="small"
  value={channelConfig.txChar}
  onChange={(e) => {
    updateChannelConfig({ txChar: e.target.value });
    setTxCharManuallyEdited(true);
  }}
  placeholder={t('gh3036.charUuidPlaceholder')}
  style={{ flex: 1 }}
/>
```

在第228-237行，修改 RX UUID Input：

```typescript
<Input
  size="small"
  value={channelConfig.rxChar}
  onChange={(e) => {
    updateChannelConfig({ rxChar: e.target.value });
    setRxCharManuallyEdited(true);
  }}
  placeholder={t('gh3036.charUuidPlaceholder')}
  style={{ flex: 1 }}
/>
```

- [ ] **Step 4: 在保存通道后重置手动编辑标记**

在 `Gh3036ChannelConfig.tsx` 第130-137行，修改 handleSaveBleChannel：

```typescript
const txSuccess = await configureTxChannel('ble', channelConfig.bleDevice, channelConfig.txChar);
if (!txSuccess) return;

const rxSuccess = await configureRxChannel('ble', channelConfig.bleDevice, channelConfig.rxChar);
if (rxSuccess) {
  await updateChannelConfig({ connectionType: 'ble' });
  // 重置手动编辑标记，允许下次自动发现
  setTxCharManuallyEdited(false);
  setRxCharManuallyEdited(false);
  message.success(t('gh3036.channelSaved'));
}
```

- [ ] **Step 5: 提交 Task 1.2 的修改**

```bash
git add src/pages/Protocol/Gh3036ChannelConfig.tsx
git commit -m "fix(gh3036): 修复BLE UUID自动复位问题"
```

---

## Task 2: 串口支持手动输入波特率

**问题分析：**
当前 `SerialSettings.tsx` 使用 Select 组件，波特率只能是预设值。需要改为 AutoComplete，允许用户输入自定义波特率。

**改动评估：**
- 前端：Select → AutoComplete（小改动）
- 类型：BaudRate 类型需要支持任意数值（中等改动）
- 后端：添加波特率范围验证（小改动）

### Task 2.1: 修改前端串口设置组件

**Files:**
- Modify: `src/pages/Serial/SerialSettings.tsx`
- Modify: `src/types/serial.ts`

- [ ] **Step 1: 修改波特率类型定义**

在 `src/types/serial.ts` 第24-28行，修改：

```typescript
// 移除固定的 BaudRate 类型，改为 number
export type BaudRate = number;

export const DEFAULT_BAUD_RATES: number[] = [
  300, 1200, 2400, 4800, 9600, 14400, 19200, 38400, 57600, 115200, 230400, 460800, 921600
];

export const BAUD_RATE_MIN = 300;
export const BAUD_RATE_MAX = 2000000; // 2M
```

- [ ] **Step 2: 修改 SerialSettings 组件**

在 `src/pages/Serial/SerialSettings.tsx` 第1-6行，修改导入：

```typescript
import React from 'react';
import { Card, Form, AutoComplete, InputNumber, Switch, Divider, Typography, Space } from 'antd';
import type { SerialConfig } from '../../types';
import { DEFAULT_BAUD_RATES, BAUD_RATE_MIN, BAUD_RATE_MAX } from '../../types';
```

在第24-34行，修改波特率输入：

```typescript
<Form.Item label="波特率">
  <AutoComplete
    value={config.baudRate.toString()}
    onChange={(value) => {
      const numValue = parseInt(value, 10);
      if (!isNaN(numValue)) {
        onUpdateConfig({ baudRate: numValue });
      }
    }}
    disabled={isConnected}
    options={DEFAULT_BAUD_RATES.map((rate) => ({
      value: rate.toString(),
      label: `${rate} bps`,
    }))}
    placeholder="选择或输入波特率"
    filterOption={(inputValue, option) =>
      option!.value.toLowerCase().indexOf(inputValue.toLowerCase()) !== -1
    }
  />
  <Text type="secondary" style={{ fontSize: 11, marginTop: 4 }}>
    支持 {BAUD_RATE_MIN} - {BAUD_RATE_MAX} bps
  </Text>
</Form.Item>
```

- [ ] **Step 3: 提交 Task 2.1 的修改**

```bash
git add src/types/serial.ts src/pages/Serial/SerialSettings.tsx
git commit -m "feat(serial): 支持手动输入波特率"
```

### Task 2.2: 添加后端波特率验证

**Files:**
- Modify: `src-tauri/src/device/serial/serial_config.rs`

- [ ] **Step 1: 添加波特率范围验证**

在 `serial_config.rs` 文件中，找到 SerialConfig 结构体的验证逻辑（如果没有则添加）：

```rust
impl SerialConfig {
    pub fn validate(&self) -> Result<(), String> {
        if self.baud_rate < 300 || self.baud_rate > 2_000_000 {
            return Err(format!(
                "波特率必须在 300 - 2000000 范围内，当前值: {}",
                self.baud_rate
            ));
        }
        if ![5, 6, 7, 8].contains(&self.data_bits) {
            return Err(format!("数据位必须是 5-8，当前值: {}", self.data_bits));
        }
        if ![1, 2].contains(&self.stop_bits) {
            return Err(format!("停止位必须是 1 或 2，当前值: {}", self.stop_bits));
        }
        Ok(())
    }
}
```

- [ ] **Step 2: 在串口打开时调用验证**

在 `commands/serial.rs` 的 open_serial 命令中，添加验证调用：

```rust
#[tauri::command]
pub async fn open_serial(
    port_name: String,
    config: SerialConfig,
    manager: State<'_, DeviceManagerRef>,
) -> Result<(), String> {
    // 添加配置验证
    if let Err(e) = config.validate() {
        return Err(e);
    }

    // 原有的打开逻辑...
}
```

- [ ] **Step 3: 提交 Task 2.2 的修改**

```bash
git add src-tauri/src/device/serial/serial_config.rs src-tauri/src/commands/serial.rs
git commit -m "feat(serial): 添加波特率范围验证"
```

---

## Task 3: 修复 BLE UUID 写入方式错误

**问题分析：**
根据错误日志，系统提示需要 "write on response"（写响应），但实际调用的是普通 "write" 方法。在 BLE 协议中，特征可以支持：
- `write`: Write with Response（需要响应）
- `write_without_response`: Write without Response（无需响应）

需要根据特征的 `properties.write` 和 `properties.write_without_response` 来选择正确的写入方式。

### Task 3.1: 修改 BLE GATT 客户端写入逻辑

**Files:**
- Modify: `src-tauri/src/device/ble/native/gatt_client.rs`

- [ ] **Step 1: 修改 write_characteristic 方法**

在 `gatt_client.rs` 第534-587行，修改 `write_characteristic` 方法：

```rust
pub async fn write_characteristic(&self, char_uuid: &str, data: &[u8]) -> Result<()> {
    let char = self.find_characteristic(char_uuid).await?;

    info!("{}", format_ble_log(&self.address, char_uuid, "W", data));
    tracing::debug!(
        "[BLE][TX_RAW] device={}, char={}, len={}, bytes={}",
        extract_short_mac(&self.address),
        extract_short_uuid(char_uuid),
        data.len(),
        format_hex_preview(data)
    );

    {
        let caches = self
            .caches
            .read()
            .map_err(|e| ComBridgeError::ble(format!("锁获取失败: {}", e)))?;
        if let Some(cache) = caches.get(char_uuid) {
            if let Err(e) = cache.tx_buffer.write(data) {
                error!("写入发送缓存失败: {}", e);
            }
        } else {
            drop(caches);
            let mut caches = self
                .caches
                .write()
                .map_err(|e| ComBridgeError::ble(format!("锁获取失败: {}", e)))?;
            let cache = CharacteristicCache::new();
            if let Err(e) = cache.tx_buffer.write(data) {
                error!("写入发送缓存失败: {}", e);
            }
            caches.insert(char_uuid.to_string(), cache);
        }
    }

    // 获取特征属性，决定使用哪种写入方式
    let props = char
        .characteristic_properties()
        .await
        .map_err(|e| ComBridgeError::ble(format!("获取特征属性失败: {}", e)))?;

    // 根据特征属性选择写入方式
    let write_result = if props.write_without_response && !props.write {
        // 特征只支持 write without response
        tracing::debug!(
            "[BLE] 使用 write_without_response: device={}, char={}",
            extract_short_mac(&self.address),
            extract_short_uuid(char_uuid)
        );
        char.write_without_response(data).await
    } else {
        // 默认使用 write with response
        tracing::debug!(
            "[BLE] 使用 write_with_response: device={}, char={}",
            extract_short_mac(&self.address),
            extract_short_uuid(char_uuid)
        );
        char.write(data).await
    };

    if let Err(e) = write_result {
        let error_str = format!("{}", e);
        if error_str.contains("已关闭")
            || error_str.contains("closed")
            || error_str.contains("disconnected")
        {
            warn!(
                "[{}] 设备已断开，写入失败: {}",
                extract_short_mac(&self.address),
                e
            );
        } else {
            error!("[{}] 写入特征失败: {}", extract_short_mac(&self.address), e);
        }
        return Err(ComBridgeError::ble(format!("写入失败: {}", e)));
    }

    Ok(())
}
```

- [ ] **Step 2: 提交 Task 3.1 的修改**

```bash
git add src-tauri/src/device/ble/native/gatt_client.rs
git commit -m "fix(ble): 根据特征属性选择正确的写入方式"
```

### Task 3.2: 添加单元测试

**Files:**
- Create: `src-tauri/src/device/ble/native/gatt_client_test.rs`（如果不存在）

- [ ] **Step 1: 创建测试文件**

创建 `src-tauri/src/device/ble/native/gatt_client_test.rs`：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_write_characteristic_selects_correct_method() {
        // 测试逻辑：验证根据特征属性选择写入方式
        // 由于需要 mock BLE 设备，这里只提供测试框架
        // 实际测试需要在集成测试环境中进行
    }

    #[test]
    fn test_write_with_response_fallback() {
        // 测试当特征支持两种写入方式时，优先使用 write with response
    }

    #[test]
    fn test_write_without_response_when_only_supported() {
        // 测试当特征只支持 write without response 时，使用该方法
    }
}
```

- [ ] **Step 2: 在 mod.rs 中添加测试模块**

在 `src-tauri/src/device/ble/native/mod.rs` 添加：

```rust
#[cfg(test)]
mod gatt_client_test;
```

- [ ] **Step 3: 提交 Task 3.2 的修改**

```bash
git add src-tauri/src/device/ble/native/gatt_client_test.rs src-tauri/src/device/ble/native/mod.rs
git commit -m "test(ble): 添加GATT客户端写入方式测试"
```

---

## Task 4: 修复波形显示不连续问题

**问题分析：**
波形显示不连续可能由以下原因造成：
1. 数据更新不及时
2. 图表渲染配置问题（animation、progressive）
3. 数据流处理有问题

需要检查：
- `MonitorTab.tsx` 的数据订阅和更新逻辑
- `MultiLineChart.tsx` 的渲染配置
- `monitorChartData.ts` 的数据构建逻辑

### Task 4.1: 检查数据更新逻辑

**Files:**
- Modify: `src/pages/Gh3036/MonitorTab.tsx`

- [ ] **Step 1: 添加数据更新调试日志**

在 `MonitorTab.tsx` 第103-110行，修改 ipdPaChartData 的 useMemo：

```typescript
const ipdPaChartData = useMemo(() => {
  const data = buildIpdPaChartData(
    currentFrames,
    sampleRate,
    ipdRawDataType,
    DISPLAY_DURATION_SECONDS
  );
  console.debug('[MonitorTab] 波形数据更新:', {
    functionId: selectedFunctionId,
    columns: data.columns.length,
    rows: data.rows.length,
    timestamp: Date.now()
  });
  return data;
}, [currentFrames, sampleRate, ipdRawDataType]);
```

- [ ] **Step 2: 提交调试修改**

```bash
git add src/pages/Gh3036/MonitorTab.tsx
git commit -m "debug(gh3036): 添加波形数据更新调试日志"
```

### Task 4.2: 优化图表渲染配置

**Files:**
- Modify: `src/pages/Waveform/MultiLineChart.tsx`

- [ ] **Step 1: 禁用动画，提升实时渲染性能**

在 `MultiLineChart.tsx` 第211行，确认 animation 已禁用：

```typescript
symbol: 'none',
animation: false,  // 禁用动画，提升实时渲染性能
```

在第257-259行，确认 progressive 配置：

```typescript
return {
  animationDuration: 0,
  progressive: 500,  // 渐进式渲染阈值
  progressiveThreshold: 3000,  // 触发渐进式渲染的数据量阈值
  // ... 其他配置
};
```

- [ ] **Step 2: 优化数据更新策略**

在 `MultiLineChart.tsx` 第422-434行，修改数据更新 useEffect：

```typescript
useEffect(() => {
  if (!initialized) return;

  // 使用防抖优化高频更新
  const updateTimer = setTimeout(() => {
    chartInstances.current.forEach((chart, index) => {
      if (!chart) return;

      const group = chartGroups[index];
      if (group) {
        const option = getChartOption(group, colorMap);
        chart.setOption(option, {
          notMerge: false,
          lazyUpdate: true  // 启用懒更新，减少渲染次数
        });
      }
    });
  }, 16);  // 约60fps的更新频率

  return () => clearTimeout(updateTimer);
}, [rows, columns, initialized, chartGroups, getChartOption, colorMap, sampleRate]);
```

- [ ] **Step 3: 提交 Task 4.2 的修改**

```bash
git add src/pages/Waveform/MultiLineChart.tsx
git commit -m "perf(gh3036): 优化波形图表渲染性能"
```

### Task 4.3: 检查数据流处理

**Files:**
- Modify: `src/pages/Gh3036/monitorChartData.ts`

- [ ] **Step 1: 添加数据有效性检查**

在 `monitorChartData.ts` 第10-42行，修改 buildIpdPaChartData：

```typescript
export function buildIpdPaChartData(
  currentFrames: Gh3036FramesPayload | null,
  sampleRate: number,
  ipdRawDataType: IpdRawDataType,
  displayDurationSeconds: number
): ChartTableData {
  if (!currentFrames || currentFrames.channel_count === 0) {
    return { columns: [], rows: [] };
  }

  const columns = Array.from(
    { length: currentFrames.channel_count },
    (_, index) => `CH${index}`
  );
  const source = ipdRawDataType === 'ipd' ? currentFrames.ipd_pa : currentFrames.rawdata;

  // 验证数据有效性
  if (!source || source.length === 0) {
    console.warn('[buildIpdPaChartData] 无有效数据源');
    return { columns, rows: [] };
  }

  const maxPoints = Math.max(1, Math.floor(displayDurationSeconds * sampleRate));
  const availablePoints = Math.min(
    currentFrames.frame_count,
    ...source.slice(0, currentFrames.channel_count).map((channel) => channel?.length ?? 0)
  );

  if (availablePoints === 0) {
    console.warn('[buildIpdPaChartData] 可用数据点为0');
    return { columns, rows: [] };
  }

  const startIndex = Math.max(0, availablePoints - maxPoints);

  const rows: number[][] = [];
  for (let frameIdx = startIndex; frameIdx < availablePoints; frameIdx++) {
    const row: number[] = [];
    for (let chIdx = 0; chIdx < currentFrames.channel_count; chIdx++) {
      const value = source[chIdx]?.[frameIdx];
      row.push(value !== undefined ? value : 0);
    }
    rows.push(row);
  }

  return { columns, rows };
}
```

- [ ] **Step 2: 提交 Task 4.3 的修改**

```bash
git add src/pages/Gh3036/monitorChartData.ts
git commit -m "fix(gh3036): 添加波形数据有效性检查"
```

### Task 4.4: 清理调试代码

**Files:**
- Modify: `src/pages/Gh3036/MonitorTab.tsx`

- [ ] **Step 1: 移除调试日志**

在 `MonitorTab.tsx` 第103-110行，移除调试日志：

```typescript
const ipdPaChartData = useMemo(() => {
  return buildIpdPaChartData(
    currentFrames,
    sampleRate,
    ipdRawDataType,
    DISPLAY_DURATION_SECONDS
  );
}, [currentFrames, sampleRate, ipdRawDataType]);
```

- [ ] **Step 2: 提交最终修改**

```bash
git add src/pages/Gh3036/MonitorTab.tsx
git commit -m "refactor(gh3036): 移除波形显示调试日志"
```

---

## 自我审查

### 1. 需求覆盖检查

✅ **问题1：BLE UUID 自动复位** - Task 1 完整覆盖
- 添加手动编辑标记
- 修改自动更新逻辑
- 在保存后重置标记

✅ **问题2：串口波特率手动输入** - Task 2 完整覆盖
- 前端改为 AutoComplete
- 类型定义支持任意数值
- 后端添加范围验证

✅ **问题3：BLE UUID 写入错误** - Task 3 完整覆盖
- 根据特征属性选择写入方式
- 添加单元测试

✅ **问题4：波形显示不连续** - Task 4 完整覆盖
- 添加数据调试日志（临时）
- 优化图表渲染配置
- 添加数据有效性检查
- 清理调试代码

### 2. 占位符扫描

✅ 无 "TBD"、"TODO"、"implement later" 等占位符
✅ 无 "Add appropriate error handling" 等模糊描述
✅ 所有代码步骤都包含完整代码
✅ 无 "Similar to Task N" 等重复引用

### 3. 类型一致性检查

✅ **Task 1** 中的 `txCharManuallyEdited` 和 `rxCharManuallyEdited` 在所有使用处一致
✅ **Task 2** 中的 `BaudRate` 类型从枚举改为 number，前后端一致
✅ **Task 3** 中的 `write_characteristic` 方法签名保持不变，内部逻辑修改
✅ **Task 4** 中的数据流类型 `ChartTableData` 在所有地方一致

---

## 执行移交

计划已完成并保存到 `docs/superpowers/plans/2026-07-29-gh3036-bug-fixes.md`。

**两种执行选项：**

**1. Subagent-Driven（推荐）** - 为每个任务派发新的子代理，任务间审查，快速迭代

**2. Inline Execution** - 在当前会话中使用 executing-plans 执行，批量执行带检查点

**选择哪种方式？**