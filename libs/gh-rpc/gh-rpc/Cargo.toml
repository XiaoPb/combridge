[package]
name = "gh-rpc"
version = "0.1.0"
edition = "2021"
description = "GH RPC business layer library"

[dependencies]
heapless = "0.8"
bitflags = "2.4"
rpc = { path = "../rpc" }
log = { version = "0.4", default-features = false }

[features]
default = ["std"]
std = []
