[package]
name = "rpc"
version = "0.1.0"
edition = "2021"
description = "RPC core functionality for gh-protocol, supports no_std"

[dependencies]
bitflags = "2.9"
heapless = "0.8"
log = { version = "0.4", default-features = false }

[features]
default = ["std"]
std = []
