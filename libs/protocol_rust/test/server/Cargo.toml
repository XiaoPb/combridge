[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
rpc = { path = "../../rpc" }
gh-rpc = { path = "../../gh-rpc" }
tokio = { version = "1", features = ["full"] }
serialport = "4"
log = "0.4"
env_logger = "0.10"
